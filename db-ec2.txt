sudo su - root

yum update -y 

yum install mysql

mysql -h <<mysql access endpoints>> -P 3306 -u admin -p



CREATE DATABASE EMP;

SHOW DATABASES;

USE EMP;

CREATE TABLE EMPLOYEE (EMP_NAME CHAR(10), EMP_ID INT, EMP_SAL INT);

DESC EMPLOYEE;

INSERT INTO EMPLOYEE (EMP_NAME, EMP_ID,EMP_SAL)VALUES ('JHON',100,20000);

INSERT INTO EMPLOYEE (EMP_NAME, EMP_ID,EMP_SAL)VALUES ('PETER',101,30000);

INSERT INTO EMPLOYEE (EMP_NAME, EMP_ID,EMP_SAL)VALUES ('PAUL',102,40000);

SELECT * FROM EMPLOYEE;